shamefully-hoist=true
strict-peer-dependencies=false
auto-install-peers=true
# Override peer dependencies for @copilotkit compatibility
# @copilotkit/runtime requires openai@^4.85.1
# @copilotkitnext/runtime requires openai@^5.9.0
# We use openai@^4.85.1 to satisfy the main requirement
# The warning for @copilotkitnext/runtime can be safely ignored
# as it's a transitive dependency and openai 4.x is compatible

