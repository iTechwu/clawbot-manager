# =============================================================================
# clawbot-manager 配置示例 (复制为 .env 并修改)
# =============================================================================

# --- 服务 ---
PORT=7100
HOST=0.0.0.0

# --- 数据目录 (本地开发模式) ---
# 本地开发时使用宿主机路径，不设置 DATA_VOLUME_NAME/SECRETS_VOLUME_NAME
# DATA_DIR=./data
# SECRETS_DIR=./secrets

# --- Docker 命名卷 (容器化部署模式) ---
# 当 API 服务运行在容器中时，需要使用命名卷来让 Bot 容器访问相同的数据
# 格式: <project-name>_<volume-name> (docker compose 会自动添加项目名前缀)
# 作用:
#   1. 告诉应用当前 /data/bots 和 /data/secrets 对应的 Docker 命名卷名称
#   2. 在容器反复重建、升级时，持久化业务数据和密钥文件
#   3. 允许其他容器或运维脚本通过卷名称安全可靠地访问同一份数据
DATA_VOLUME_NAME=clawbot-manager_clawbot-data
SECRETS_VOLUME_NAME=clawbot-manager_clawbot-secrets

# --- Bot 运行时 ---
BOTENV_IMAGE=clawbot-env:latest
OPENCLAW_BASE_IMAGE=1panel/openclaw:latest
OPENCLAW_GIT_TAG=main
BOT_PORT_START=19000

# --- Docker 构建 (镜像地址由 .env 配置，docker-compose 传入) ---
# linux/arm64: Apple Silicon; linux/amd64: x86 服务器
DOCKER_PLATFORM=linux/arm64
# Node 基础镜像 (api/web)
# 国内: uhub.service.ucloud.cn/pardx/node:24.1-slim
BASE_NODE_IMAGE=node:24.1-slim
# OpenClaw 基础镜像 (botenv，已在上方 Bot 运行时 配置)
# 国内: uhub.service.ucloud.cn/pardx/openclaw:latest
