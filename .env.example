# =============================================================================
# clawbot-manager 配置示例 (复制为 .env 并修改)
# =============================================================================

# --- 服务 ---
PORT=7100
HOST=0.0.0.0

# --- 数据目录 (本地开发) ---
DATA_DIR=./data
SECRETS_DIR=./secrets

# --- Docker 卷名 ---
DATA_VOLUME_NAME=clawbot-manager-data
SECRETS_VOLUME_NAME=clawbot-manager-secrets

# --- Bot 运行时 ---
BOTENV_IMAGE=clawbot-manager-env:latest
OPENCLAW_BASE_IMAGE=1panel/openclaw:latest
OPENCLAW_GIT_TAG=main
BOT_PORT_START=19000

# --- Docker 构建 (镜像地址由 .env 配置，docker-compose 传入) ---
# linux/arm64: Apple Silicon; linux/amd64: x86 服务器
DOCKER_PLATFORM=linux/arm64
# Node 基础镜像 (api/web)
# 国内: uhub.service.ucloud.cn/pardx/node:24.1-slim
BASE_NODE_IMAGE=node:24.1-slim
# OpenClaw 基础镜像 (botenv，已在上方 Bot 运行时 配置)
# 国内: uhub.service.ucloud.cn/pardx/openclaw:latest
